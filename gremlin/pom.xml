<?xml version="1.0" encoding="UTF-8"?>
<!--
    Copyright © 2021-present Arcade Data Ltd (info@arcadedata.com)

    Licensed under the Apache License, Version 2.0 (the "License");
    you may not use this file except in compliance with the License.
    You may obtain a copy of the License at

        http://www.apache.org/licenses/LICENSE-2.0

    Unless required by applicable law or agreed to in writing, software
    distributed under the License is distributed on an "AS IS" BASIS,
    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
    See the License for the specific language governing permissions and
    limitations under the License.

    SPDX-FileCopyrightText: 2021-present Arcade Data Ltd (info@arcadedata.com)
    SPDX-License-Identifier: Apache-2.0
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.arcadedb</groupId>
        <artifactId>arcadedb-parent</artifactId>
        <version>26.9.1-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>arcadedb-gremlin</artifactId>
    <packaging>jar</packaging>
    <name>ArcadeDB Gremlin</name>

    <properties>
        <!-- gremlin.version is defined in the parent pom so arcadedb-gremlin-it can share it -->
        <translation.version>1.0.4</translation.version>
        <snakeyaml.version>2.7</snakeyaml.version>
        <netty.version>4.2.17.Final</netty.version>
        <commons-configuration2.version>2.15.1</commons-configuration2.version>
        <commons-beanutils.version>1.11.0</commons-beanutils.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <ivy.version>2.6.0</ivy.version>
        <!-- Both properties below are dictated by TinkerPop, not chosen by ArcadeDB.
             Do not raise either one without upgrading gremlin.version first; see #5535. -->

        <!-- Pinned to the 4.0.x line because gremlin-groovy is built against it
             (TinkerPop 3.8.1 itself declares groovy.version 4.0.25). Groovy 5 is a
             major break and cannot be adopted until TinkerPop moves.
             4.0.x patch and minor updates ARE wanted, and the value deliberately runs
             ahead of TinkerPop's own 4.0.25. Dependabot enforces exactly that split:
             .github/dependabot.yml ignores org.apache.groovy majors only. -->
        <groovy.version>4.0.33</groovy.version>

        <!-- Pinned to 4.9.1 because gremlin-language ships a Gremlin grammar parser
             generated by the ANTLR 4.9.1 tool, and ANTLR 4.10 changed the serialized
             ATN format: the BASE_SERIALIZED_UUID field present in 4.9.1's
             ATNDeserializer does not exist in 4.13.2, so a 4.13 runtime cannot
             deserialize a 4.9.1-generated parser. The antlr4-runtime dependency below
             is additionally marked optional=true for shading reasons (#5216 / #5217),
             which assumes this pinned version.

             This property is module-local and intentionally diverges from
             engine/pom.xml, which tracks the latest ANTLR (4.13.2) for the engine's
             own grammar. There is deliberately NO Dependabot ignore rule for ANTLR:
             ignore rules match dependency coordinates rather than modules, and 4.9.1
             sits below the engine's 4.13.2, so any blocking range would freeze the
             engine too. Gremlin ANTLR bump PRs are closed by hand instead. -->
        <antlr4.version>4.9.1</antlr4.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>com.arcadedb</groupId>
            <artifactId>arcadedb-server</artifactId>
            <version>${project.parent.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.arcadedb</groupId>
            <artifactId>arcadedb-server</artifactId>
            <version>${project.parent.version}</version>
            <scope>test</scope>
            <type>test-jar</type>
        </dependency>
        <dependency>
            <groupId>com.arcadedb</groupId>
            <artifactId>arcadedb-integration</artifactId>
            <version>${project.parent.version}</version>
            <scope>compile</scope>
        </dependency>
        <!-- ArcadeGraph, ArcadeGraphFactory and ArcadeGremlin reference com.arcadedb.remote.RemoteDatabase
             on the ordinary embedded query path (an instanceof check that fails to link if the class is
             missing), not only for remote usage. arcadedb-server is provided-scope above (not transitive
             to consumers), so this must be declared explicitly at compile scope or every embedded-only
             consumer of the published arcadedb-gremlin artifact fails with NoClassDefFoundError on the
             first query (issue #5879). arcadedb-network itself only depends on arcadedb-engine. -->
        <dependency>
            <groupId>com.arcadedb</groupId>
            <artifactId>arcadedb-network</artifactId>
            <version>${project.parent.version}</version>
            <scope>compile</scope>
        </dependency>

        <!-- Explicit ANTLR runtime to ensure consistent version across legacy and native Cypher.
             optional=true: keeps antlr 4.9.1 on gremlin's own classpath and bundled+relocated in
             the shaded uber-jar (maven-shade includes optional deps), but does NOT export it to
             Maven-coordinate consumers. A both-modules consumer therefore resolves antlr to the
             engine's 4.13.2 only, and consumes the arcadedb-gremlin:shaded classifier at runtime
             (issue #5217). Must NOT be <scope>provided</scope>: shade drops provided deps, which
             would strip the relocated antlr from the shaded jar and regress #5216. -->
        <dependency>
            <groupId>org.antlr</groupId>
            <artifactId>antlr4-runtime</artifactId>
            <version>${antlr4.version}</version>
            <optional>true</optional>
        </dependency>

        <!-- Jackson, bundled+relocated in the shaded jar in place of TinkerPop's own copy (issue #7097).
             org.apache.tinkerpop:gremlin-shaded carries a relocated Jackson at whatever version TinkerPop
             was released with (2.15.2 for 3.8.1) TOGETHER WITH Jackson's original META-INF/maven metadata,
             so every SBOM/CVE scanner pointed at the distribution reports jackson-core 2.15.2 against
             arcadedb-gremlin-*-shaded.jar while lib/ ships the BOM-managed version. The shade below drops
             TinkerPop's copy and rebuilds the very same relocation from this dependency, so the bytecode
             that runs, the metadata a scanner reads and the plain Jackson next to it are one version.
             Optional for the same reason as antlr4-runtime above: bundled+relocated only, never leaked to
             Maven consumers, for whom gremlin-shaded still provides org.apache.tinkerpop.shaded.jackson.
             ShadedJarLayoutTest.bundledJacksonIsTheBomVersionAndItsMetadataSaysSo is the guard. -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
            <version>${netty.version}</version>
        </dependency>
        <!-- explicit import to solve CVE-2025-48924-->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons-lang3.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-configuration2</artifactId>
            <version>${commons-configuration2.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-beanutils</groupId>
            <artifactId>commons-beanutils</artifactId>
            <version>${commons-beanutils.version}</version>
        </dependency>
        <!-- ARCADEDB DOES NOT USE THIS LIBRARY BUT GREMLIN DOES. REPLACING IT WITH THE LAST RELEASE BECAUSE OF THE SECURITY ISSUE
 https://nvd.nist.gov/vuln/detail/CVE-2022-1471 -->
        <dependency>
            <groupId>org.yaml</groupId>
            <artifactId>snakeyaml</artifactId>
            <version>${snakeyaml.version}</version>
        </dependency>

        <!-- ARCADEDB DOES NOT USE THIS LIBRARY BUT GREMLIN DOES: gremlin-groovy pulls Ivy in to
             back Groovy's Grape dependency loader. TinkerPop 3.8.1 resolves 2.5.3, which is
             affected by the PackagerResolver path traversal
             https://nvd.nist.gov/vuln/detail/CVE-2026-26032 -->
        <dependency>
            <groupId>org.apache.ivy</groupId>
            <artifactId>ivy</artifactId>
            <version>${ivy.version}</version>
        </dependency>

        <!-- Groovy core is declared directly so its version is pinned at depth 1. It also arrives
             transitively from groovy-groovysh (4.0.33) and from gremlin-groovy (4.0.25), both at
             depth 2, so without this declaration Maven picks the winner by DECLARATION ORDER:
             moving the gremlin-groovy block above groovy-groovysh silently drops the whole
             uber-jar back to Groovy 4.0.25. A silent version divergence of exactly that kind is
             what issue #6771 was, so the pin is explicit rather than positional. -->
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy</artifactId>
            <version>${groovy.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy-groovysh</artifactId>
            <version>${groovy.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>jline</groupId>
                    <artifactId>jline</artifactId>
                </exclusion>
                <!-- groovy-console drags in groovy-swing, i.e. an AWT/Swing UI that a headless
                     server never opens. It also ships META-INF/groovy/org.codehaus.groovy.runtime.ExtensionModule.
                     That descriptor is a single-module properties file at a fixed path, so a
                     consumer that resolves groovy-swing normally (gremlin-groovy pulls 4.0.25)
                     ends up with two descriptors naming module groovy-swing at different versions
                     and Groovy aborts with "Conflicting module versions" (issue #6771). Dropping
                     groovy-swing removes the descriptor, and with it the conflict. -->
                <exclusion>
                    <groupId>org.apache.groovy</groupId>
                    <artifactId>groovy-console</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy-jsr223</artifactId>
            <version>${groovy.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy-json</artifactId>
            <version>${groovy.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-core</artifactId>
            <version>${gremlin.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>commons-beanutils</groupId>
                    <artifactId>commons-beanutils</artifactId>
                </exclusion>
                <!-- gremlin-core pulls antlr4-runtime 4.9.1 transitively via gremlin-language,
                     independently of and in addition to the explicit (optional) antlr4-runtime
                     dependency declared above. That transitive edge is not optional and is not
                     affected by marking our own duplicate declaration optional, so it still leaks
                     4.9.1 to Maven-coordinate consumers (issue #5217) unless excluded here. The
                     explicit optional dependency above continues to supply antlr4-runtime for
                     gremlin's own compilation and for bundling+relocation in the shaded jar. -->
                <exclusion>
                    <groupId>org.antlr</groupId>
                    <artifactId>antlr4-runtime</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-server</artifactId>
            <version>${gremlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-driver</artifactId>
            <version>${gremlin.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-util</artifactId>
            <version>${gremlin.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-groovy</artifactId>
            <version>${gremlin.version}</version>
        </dependency>

        <dependency>
            <groupId>org.apache.tinkerpop</groupId>
            <artifactId>gremlin-test</artifactId>
            <version>${gremlin.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.vintage</groupId>
            <artifactId>junit-vintage-engine</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>test-jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- Gremlin's tests exercise BOTH the engine SQL/Cypher parser (antlr 4.13.2)
                         and TinkerPop's parser (antlr 4.9.1); those cannot coexist on the
                         unshaded test classpath. They run in the arcadedb-gremlin-it module
                         against the shaded jar instead. Test sources still compile here so the
                         test-jar is produced. To run a single gremlin test, target gremlin-it, e.g.
                         mvn -pl gremlin-it test -Dtest=GremlinTest (or -Dit.test=... for *IT). -->
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- Same rationale as the surefire skip above: the *IT classes inherited from
                         the parent failsafe config would otherwise run on the unshaded classpath
                         (engine antlr 4.13.2 vs gremlin antlr 4.9.1) under -Pintegration, which CI
                         uses. They run in arcadedb-gremlin-it against the shaded jar instead. -->
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <configuration>
                    <!-- Every third-party library bundled here is relocated into a private package, so a
                         consumer can pin their own version of it without the uber-jar's copy silently
                         outvoting theirs (issue #6793). ShadedJarLayoutTest enforces that; its
                         ALLOWED_UNRELOCATED list carries the reason for each deliberate exception.

                         Two of those exceptions are worth repeating here, because both were learnt the
                         hard way and both look like omissions:

                         - GROOVY CANNOT BE RELOCATED. maven-shade rewrites string constants without
                           respecting package boundaries, so a "groovy" pattern also rewrites ".groovy"
                           (the script extension), "groovy.target.indy" (a system property) and
                           "groovydoc". The Groovy compiler then dies on its own script class name with
                           "ASM reporting processing error ... Script1.com.arcadedb.gremlin.shaded.groovy",
                           which reaches the user as
                           "MissingPropertyException: No such property: metaClass for class: java.util.Map"
                           while TinkerPop's ObjectLoader.groovy initialises the engine. Its META-INF
                           descriptors compound this: they name classes in plain text that shade does not
                           rewrite. GremlinGroovyEngineTest is the guard.
                         - commons-configuration2 must keep its coordinates because TinkerPop's PUBLIC API
                           takes those types, e.g. GraphFactory.open(Configuration). Relocating it turns
                           every such call into a NoSuchMethodError. Same for org.javatuples, which
                           TraversalExplanation returns. -->
                    <relocations>
                        <!-- ANTLR 4.9.1: TinkerPop's precompiled parser needs the v3 ATN format, so it must
                             never collide with the engine's org.antlr 4.13.2 on a shared classpath. -->
                        <relocation>
                            <pattern>org.antlr</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.antlr</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>io.netty</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.io.netty</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.jctools</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.jctools</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.apache.commons</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.apache.commons</shadedPattern>
                            <excludes>
                                <exclude>org.apache.commons.configuration2.**</exclude>
                            </excludes>
                        </relocation>
                        <relocation>
                            <pattern>com.github</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.com.github</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.carrotsearch</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.com.carrotsearch</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.codahale</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.com.codahale</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.yaml</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.yaml</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.apache.ivy</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.apache.ivy</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>nl.altindag</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.nl.altindag</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>net.objecthunter</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.net.objecthunter</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.mindrot</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.org.mindrot</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.squareup.javapoet</pattern>
                            <shadedPattern>com.arcadedb.gremlin.shaded.com.squareup.javapoet</shadedPattern>
                        </relocation>
                        <!-- Jackson at the BOM version, rebuilt onto TinkerPop's OWN relocation prefix (issue #7097):
                             gremlin-core and gremlin-util were compiled against org.apache.tinkerpop.shaded.jackson
                             and are not rewritten here, so the replacement must land exactly where TinkerPop's
                             2.15.2 copy used to be. That copy is dropped by the gremlin-shaded filter below.
                             The multi-release overlays need their own patterns because shade rewrites the class
                             bodies under META-INF/versions but not their paths; the levels listed are the ones
                             jackson-core ships classes at today (its versions/9 holds only the module-info that
                             the filter below drops), and everyBundledThirdPartyPackageIsRelocated fails on a
                             new one, since com/fasterxml is not on its allow-list. -->
                        <relocation>
                            <pattern>com.fasterxml.jackson</pattern>
                            <shadedPattern>org.apache.tinkerpop.shaded.jackson</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>META-INF/versions/11/com.fasterxml.jackson</pattern>
                            <shadedPattern>META-INF/versions/11/org.apache.tinkerpop.shaded.jackson</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>META-INF/versions/17/com.fasterxml.jackson</pattern>
                            <shadedPattern>META-INF/versions/17/org.apache.tinkerpop.shaded.jackson</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>META-INF/versions/21/com.fasterxml.jackson</pattern>
                            <shadedPattern>META-INF/versions/21/org.apache.tinkerpop.shaded.jackson</shadedPattern>
                        </relocation>
                    </relocations>
                    <!-- Groovy's extension-module descriptor is a SINGLE-module properties file at a
                         fixed path, so it cannot be merged: of the modules bundled here only one
                         copy survives the shade, and the survivor then advertises its own
                         moduleVersion to every classpath the uber-jar lands on. A consumer that
                         also resolves that module normally (gremlin-groovy 3.8.1 pulls groovy 4.0.25)
                         gets two descriptors for the same moduleName at different versions and Groovy
                         aborts at startup with "Conflicting module versions" (issue #6771). Nothing
                         Gremlin does needs the extension methods these descriptors register
                         (XmlExtensions, ScriptExtensions), so the uber-jar publishes none of them
                         and the conflict cannot arise. -->
                    <filters>
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/groovy/org.codehaus.groovy.runtime.ExtensionModule</exclude>
                                <exclude>META-INF/services/org.codehaus.groovy.runtime.ExtensionModule</exclude>
                                <!-- The shade plugin does not relocate the multi-release overlay, so
                                     snakeyaml's Java 9 copy of org.yaml.snakeyaml.internal.Logger
                                     survives under its ORIGINAL name while the base copy is
                                     relocated. On Java 9+ that orphan shadows a consumer's own
                                     snakeyaml (issue #6793); nothing in the uber-jar can reach it,
                                     because every reference was rewritten. Same for the bundled
                                     module-info descriptors, which still export the pre-relocation
                                     packages. -->
                                <exclude>META-INF/versions/*/org/yaml/**</exclude>
                                <exclude>META-INF/versions/*/module-info.class</exclude>
                                <exclude>module-info.class</exclude>
                            </excludes>
                        </filter>
                        <!-- TinkerPop's bundled Jackson (2.15.2 for 3.8.1) and the Maven metadata that made every
                             scanner report it (issue #7097): both are rebuilt from the BOM-managed jackson-databind
                             dependency by the relocation above, so nothing of TinkerPop's copy may survive, or
                             shade keeps whichever class it met first and the uber-jar runs a mix of two Jacksons.
                             Kryo, minlog and objenesis stay: TinkerPop relocates those into the same jar and no
                             scanner has anything to say about them. -->
                        <filter>
                            <artifact>org.apache.tinkerpop:gremlin-shaded</artifact>
                            <excludes>
                                <exclude>org/apache/tinkerpop/shaded/jackson/**</exclude>
                                <exclude>META-INF/versions/*/org/apache/tinkerpop/shaded/jackson/**</exclude>
                                <exclude>META-INF/maven/com.fasterxml.jackson.core/**</exclude>
                            </excludes>
                        </filter>
                    </filters>
                    <artifactSet>
                        <excludes>
                            <!-- Do NOT bundle the engine: its SQL/Cypher parsers are v4-ATN (antlr 4.13.2) and the
                                 org.antlr relocation above would rewrite them onto the bundled 4.9.1 runtime, making
                                 them undeserializable. The engine ships as its own plain jar (distribution lib/ and
                                 the arcadedb-gremlin-it module both provide it), so the relocation must touch only
                                 TinkerPop's own 4.9.1 parser. -->
                            <exclude>com.arcadedb:arcadedb-engine</exclude>
                            <!-- Do NOT bundle network either: it is a real compile-scope dependency (#5879),
                                 so Maven resolves it transitively for every consumer of either the plain or the
                                 shaded jar. Bundling it here as well would duplicate its classes on the classpath
                                 of any consumer who, like arcadedb-server or arcadedb-gremlin-it, also pulls the
                                 plain arcadedb-network jar directly. -->
                            <exclude>com.arcadedb:arcadedb-network</exclude>
                            <!-- Same for integration, which the GraphML/GraphSON format handlers in
                                 src/main/java compile against and which is therefore compile-scope too. -->
                            <exclude>com.arcadedb:arcadedb-integration</exclude>
                            <!-- The server is provided-scope, so shade drops it anyway; the exclude states the
                                 intent, because the server is the assembly point that loads this jar. -->
                            <exclude>com.arcadedb:arcadedb-server</exclude>
                            <!-- ...and neither the engine's own dependency tree. Excluding an artifact does NOT
                                 exclude what it drags in, so before this list the uber-jar carried a full unrelocated
                                 copy of Lucene, GraalJS/Truffle, gson and friends: ~50MB that nothing inside the jar
                                 can even reach, because the engine that uses them is not here. For a consumer they
                                 are worse than dead weight - a bundled Lucene silently outvotes their own newer one
                                 and the first mismatched call dies with NoSuchMethodError, which is exactly what
                                 issue #6793 reports. Every artifact below travels with arcadedb-engine, so the
                                 consumer already has it, at the version the engine asked for.
                                 ShadedJarLayoutTest fails if a new one appears and is not listed here. -->
                            <exclude>at.yawk.lz4:*</exclude>
                            <exclude>com.conversantmedia:*</exclude>
                            <exclude>com.google.code.gson:*</exclude>
                            <exclude>com.google.errorprone:*</exclude>
                            <exclude>io.github.jbellis:*</exclude>
                            <exclude>io.sgr:*</exclude>
                            <exclude>org.agrona:*</exclude>
                            <exclude>org.apache.commons:commons-math3</exclude>
                            <exclude>org.apache.lucene:*</exclude>
                            <exclude>org.graalvm.js:*</exclude>
                            <exclude>org.graalvm.polyglot:*</exclude>
                            <exclude>org.graalvm.regex:*</exclude>
                            <exclude>org.graalvm.sdk:*</exclude>
                            <exclude>org.graalvm.shadowed:*</exclude>
                            <exclude>org.graalvm.truffle:*</exclude>
                            <exclude>org.locationtech.jts:*</exclude>
                            <exclude>org.locationtech.spatial4j:*</exclude>
                            <!-- ...and the same for arcadedb-integration's tree. -->
                            <exclude>com.sonofab1rd:univocity-parsers</exclude>
                        </excludes>
                    </artifactSet>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
