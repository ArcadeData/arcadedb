# NPM Configuration for ArcadeDB Studio
# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=true
progress=true

# Registry settings (use default npm registry)
registry=https://registry.npmjs.org/

# Dependency resolution
# Required for jQuery 4.0 compatibility with older packages
# that have overly conservative peer dependency constraints
legacy-peer-deps=true
